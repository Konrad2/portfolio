<?php
	
//Wy��wietlanie po��sz�czonych modu��w
/**
 * @todo polaczony modul nie z palca. 
 */


/*
	if ( ! empty( $this->mod ) )
	
		foreach ( $this->mod as $m ) {
				
		//	echo "view one jestem</br>";
			echo $this->action('viewlabel', 
								'view',
								$m,
								array('param' => $this->id) );
		}
*/			
 //WyŶwietlanie wiersza
 
	echo "<table>";
	 
	  	$t = $this->thing->toArray();
	  	
	  
		
		echo '<h1>'. $t['name'] .'</h1>';
	
	  //	foreach ($t as $key => $item): 
	 foreach ( $this->keysAndLabels as $key => $label ) {
	  	//if ($key != 'id'){
	  		  	
			echo"<tr>";
				
				echo '<td>'. $label .'</td>';
				
				$out =  empty ( $t [ $key ] ) ? 'b.d' : $t [ $key ];
				 
				echo '<td>'.$this->escape($out).'</td>';
				
			echo"</tr>";
	  //	}
	 }
	  
	   
	echo "</table>"; 
	
		echo '<a href="/'.$this->module.'/'.$this->controller.'/viewall">powrót</a>';
?>