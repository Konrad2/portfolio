<?php

class courier extends  abstract_formDbModelParent
{
	
	
	
	
	/**
	 * Dodaje kuriera do bazy danych
	 * @param unknown_type $row
	 * @param unknown_type $data
	 * @param unknown_type $values
	 * @return Zend_Db_Table_Row dodany rekord.
	 */
/*	public function doInsert($row = null, $data = null, $values = null){
		
		$newRow = $this->createRow();
		
		foreach ($newRow->toArray() as $key => $v){

			$newRow[$key] = $data[$key];
		}
		
		$newRow->save();
		
		return $newRow;
	}
	*/
}
